# Audit: `sections/04-theory-files.tex` (Theory Files and Serialization)

**Verdict: accurate (minor-issues).** The chapter is exceptionally faithful to the
code. Every named function/class/method/file exists; every line-range citation I
spot-checked into `pipeline/theory_serialize.py`, `notebooks/daedalus.py`,
`pipeline/_diagrams.py`, `msrjd/core/serialize.py`, and `pipeline/_expand_cache.py`
landed on the right region (and usually the exact line). The reproduced
`ou_quartic.theory.py` / `ou_quartic_colored.theory.py` source matches verbatim,
the KPZ action string matches verbatim, the binary-cache key format
(`unique_typed_mult_v3_dx1_dx1_taylor6_k2_l2`) matches the real producer and the
real on-disk `saved_theories/ou_quartic_white_noise/manifest.json`.

**LaTeX: valid (latex_ok = true).** Master `daedalus_manual.tex` `\input`s this
chapter (line 182); all macros used (`\code \file \term \msrjd \dd \ee \Dt \avg`)
and environments (`note`, `gotcha`, `defn` via `\newtcolorbox`; `longtable`,
`booktabs`, `tikz`+`arrows.meta,positioning,calc`) are defined in the preamble.
The built PDF (2026-06-18T14:44) is **newer** than the chapter source
(2026-06-18T09:54), and `daedalus_manual.log` shows the chapter rendering
(`./sections/04-theory-files.tex [47] Chapter 4.`) with **no** `! `/`LaTeX Error`/
`Undefined control sequence`/`Runaway argument`. Special chars (`_ # %`) appear
only inside `\code{...\_...}`, listings, or math.

---

## Findings

- **nit** — "same trimming describe_model performed" conflates two distinct
  functions.
  - *Manual claim* (lines 848-849): `_strip_doc_boilerplate` is "the same trimming
    `describe_model` performed in Chapter~\ref{ch:quickstart}."
  - *Code reality*: `describe_model` (`notebooks/daedalus.py:138`) trims via a
    *different* helper, `_doc_physics` (`daedalus.py:125`), driven by its own
    `_DOC_DROP`/`_DOC_STOP` lists — NOT `_strip_doc_boilerplate`
    (`theory_serialize.py:873`). The two are analogous in *effect* (both drop the
    "<name> — text-driven theory file." / "Generated by" / "Loaded by" header) but
    are separate implementations with different drop-lists. Reads as an
    effect-level analogy, so harmless, but a reader could infer shared code.
  - *Location*: `04-theory-files.tex:848-849` vs `notebooks/daedalus.py:125,138`.

- **nit** — `_slugify` regex quoted without the empty-string fallback.
  - *Manual claim* (lines 727-728): `_slugify(name)` is
    `re.sub(r'[^A-Za-z0-9]+', '_', name).strip('_').lower()`.
  - *Code reality* (`theory_serialize.py:98`): identical, but with a trailing
    `or 'theory'` (so an all-symbol name slugs to `'theory'` rather than `''`).
    Abbreviation, not an error.
  - *Location*: `04-theory-files.tex:727-728` vs `pipeline/theory_serialize.py:98`.

- **nit** — illustrative `manifest.json` timestamp is not a real entry from the
  cited file.
  - *Manual claim* (lines 972-976): "A representative `manifest.json` entry (from
    `saved_theories/ou_quartic_white_noise/`)" with
    `"saved_at": "2026-06-17T23:03:39.062450"`.
  - *Code reality*: the real entries in that file carry `2026-06-15T18:27..28..`
    timestamps; the entry is labeled "representative," the directory mtime is
    indeed Jun-17 23:03, and the **schema** (`key`/`stage`/`k`/`loop_order`/
    `saved_at`) and the **example key** `unique_typed_mult_v3_dx1_dx1_taylor6_k2_l2`
    are exactly correct (verified against the on-disk manifest). Cosmetic only.
  - *Location*: `04-theory-files.tex:972-976` vs
    `saved_theories/ou_quartic_white_noise/manifest.json`.

---

## Spot-checks that PASSED (non-exhaustive, for the record)

- Builder-split inference listing (`_is_spatial = any(... spatial_dim ...)`,
  ch. 427-429) == `theory_serialize.py:370-372` verbatim.
- Saddle-param filter ch.334-339, `mean_field` double-guard at `:181` — both exact.
- Toggle emit-only-when-non-default gates: stability `:439-440`, markovianize
  `:448-449`, operator_ir `:452-459`, boundary/initial `:466-477`, dyson/refdiff
  `:484-489` — all exact.
- Equation-preference `if equations: ... else: mf_equations` at `:424-433` — exact.
- AST snippet lines (import ast @545, `ast.parse` @550, `ast.get_docstring` @553,
  `ast.literal_eval` @568) — all exact.
- Reverse chain-unwind `:588-598`, n_populations sync `:868-869`, the two
  `ValueError` raises `:581`/`:585`, constructor accepts all 3 class names
  `:651-652`, `_default_spatial_dim` inherit `:682-683` — all exact.
- No-arg toggle defaults: `markovianize()`→True (`:790`), `operator_ir()`→True
  (`:802`); builder signatures `operator_ir(on=True)` (theory.py:1053),
  `markovianize(enabled=True)` (theory.py:429) match the reverse-path kwarg checks.
- `_emit_cgf_term`: multi-leg vs legacy-positional shapes, `int(order)`, bool-only
  `markovianize=` (`:256-274`) — exact.
- `_py_repr` bool-before-int ordering at `:54` — exact.
- `save_theory_to_file` path logic `:513-518` (dir → slug; non-`.theory.py` →
  append unless `.py`) — exact; the `list_theories` discovery filter
  `daedalus.py:63-65` (`f.endswith('.theory.py')`) — exact.
- `daedalus.load_theory` (`daedalus.py:68`), `list_theories` (`:61`),
  `THEORIES_DIR` (`:55`), `repo_root` walk-to-`pipeline/` (`:40`) — all exact;
  exec snippet (spec_from_file_location/module_from_spec/exec_module) verbatim.
- `msrjd/core/serialize.py` has its own `save_theory`/`load_theory`/`reload_model`;
  `reload_model` uses `exec(compile(...))` (`:237`) — chapter's "raw exec" correct;
  save format `metadata.json` + `symbolic_data.sobj` (`:9-10`,`:130`,`:150`) — exact.
- Cache key `v3` (chapter) matches the REAL producer `_diagrams.py:150`
  (`unique_typed_mult_v3_{ext_tag}_taylor{N}`); the stale `v1` example in
  `_expand_cache.py:41` is a docstring lag, so the chapter is RIGHT to say v3.
- Slug `'OU Quartic (white noise)'` → `saved_theories/ou_quartic_white_noise/`
  confirmed on disk.
- `define_function('phi', args=['v'], expression='a[i]*v^2', population='E')`
  example matches `quadratic_hawkes_alpha.theory.py:47` /
  `single_population_conductance_test.theory.py:26` verbatim.
- `linear_hawkes` DEFAULT_FUNDAMENTAL `{'E':[0.78,0.81],'w':[[0.30,0.25],
  [0.30,0.35]],...}` verbatim; `quadratic_hawkes_alpha` DEFAULT_FUNDAMENTAL `{}`
  confirmed (`:63`); `ou_quartic_colored` uses `n_populations=0` + `indexed=True`.
- `_collect()` is a real form method (`pipeline/ui/main.py:2335`) — diagram label OK.
- `pipeline/colored_to_markovian.py` exists (Markovianization embedding claim OK).
- `g_eff ≈ 0.02` comment at `ou_quartic.theory.py:34` — exact.
