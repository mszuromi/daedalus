# Pipeline cleanup audit (June 2026)

Multi-agent audit of the temporal + spatial MSR-JD pipelines for dead code, duplication, and safe simplifications (never sacrificing functionality). 11 parallel finders + adversarial verification of every high-value finding.

**65 findings** — by category: {'dead_code': 14, 'simplification': 18, 'duplication': 21, 'docs': 3, 'complexity': 6, 'inconsistency': 3}; by severity: {'medium': 19, 'low': 44, 'high': 2}.

## Applied (verified real + safe + mechanical) — commit 5565192
- **heat_kernel.py:287-306** (dead_code) — Remove the unused parameter: change signature from '_make_numeric(expr, free_symbol_names)' to '_make_numeric(expr)' and drop it from all three call sites (lines 461, 462, 465). The parameter was like
- **field_theory.py:1010-1013** (docs) — Update the docstring: 'Return the (1,1) sector polynomial, or R.zero() if absent.' This clarifies the actual behavior and matches the implementation.
- **_propagator.py:1048-1056, 1251-1258, 1546-1551** (duplication) — Extract _horner() to module level (outside compute_poles_and_residues) as a standalone helper function. Update all three call sites to use the shared version. The function signature is identical acros
- **theory.py:561, 680** (duplication) — Delete the first `use_action_template` method at lines 561-574 (`_TemporalMethods.use_action_template`). Keep the second definition at lines 680-695 (`_BaseTheoryBuilder.use_action_template`), which i

## Deferred — needs-human (real duplication, but invasive refactors; left as suggestions)
- **full_integrator.py:517-536, 918-934** — Extract a helper function `_make_schwinger_grid(n_C, n_s, mu_or_scale, fold_exp=True)` that returns (s_nodes, s_w, s_flat, s_wflat, Ps). Use fold_exp=True for diagram_kinematic (current behavior) and fold_exp=False for diagram_kinematic_spe
- **full_integrator.py:580-603, 941-963** — Extract a helper function `_build_causal_grid(order, internal, idx, s_up, s_lo, external_times, n_t, s_flat, s_wflat)` that returns (placed, wt, tvals, sig, wfull, Pt, Ps, P). Call it from both functions. Eliminates ~24 lines of duplication
- **final_integral.py:2047-2105 (and similar at 2120-2172)** — Extract the common pole-tuple processing logic into a helper function `_process_1d_pole_tuple(C_prod, alpha_s_val, gamma, pref, L, U, L_inf, U_inf)` that returns contrib or None. Call this helper from both code paths with appropriate pre-co
- **final_integral.py:2238-2273 (and similar at 2275-2330)** — Extract the common per-tuple polygon-integration logic: `_process_2d_polygon_pole_tuple(C_prod, alpha_s_val, beta_s_val, gamma, pref, triangles)` → contrib or None. Call from both paths. The triangle loop is identical in both, so consolidat
- **grouped_integral.py:150-176, 207-228** — Extract a shared helper `def _resolve_constraints(subset_constraint_data, free_ext_vals): ...` that returns pre-computed constraint-resolved values (c_eff per edge, feasibility flags), then call it from both m0 and m1. This avoids repeating
- **_propagator.py:1477-1487, 1564-1572** — Define a single _cofactor_adj() function at module level (or in a shared helper scope) before compute_poles_and_residues(). Remove the duplicate _cofactor_adj_lean() definition and replace its call site (line 1576) with a call to the shared
- **theory_compiler.py:1540-1600** — Factor out the common kernel-spec walking and indexing-resolution logic into a shared `_evaluate_kernel_specs(kernel_specs, param_names, is_frequency_domain)` helper. Pass a callback that handles either FT or TD evaluation. This reduces ~18

## All findings (for reference)
- [high/duplication/risk:none/conf:high] **theory.py:561, 680** — The method `use_action_template(self, template)` is defined twice in the same class hierarchy. Line 561 is defined in `_TemporalMethods` mixin; line 680 is redefined in `_BaseTheoryBuilder` (or later 
- [high/duplication/risk:low/conf:high] **theory_compiler.py:1540-1600** — `make_kernel_ft_image_lambda()` (lines 1493-1600) and `make_kernel_td_image_lambda()` (lines 1603-1681) are near-identical. Both walk the kernel_specs list, resolve indexing shapes (scalar / vector / 
- [low/simplification/risk:none/conf:medium] **cache.py:62-67** — _to_int() wraps a simple None-check and int() coercion. Called only twice (in _stage_key()), it adds unnecessary indirection for a one-liner pattern.
- [low/dead_code/risk:none/conf:high] **field_theory.py:75-92** — inverse_fourier_transform() is defined but never referenced anywhere in the codebase. Only fourier_transform() is actively imported and used by _propagator.py. The inverse FT function appears to be a 
- [low/dead_code/risk:none/conf:high] **field_theory.py:104-179** — Conv, IP, _ConvIPBase, _Neg, and _DisplaySum classes are display helpers for rendering convolution and inner-product expressions as LaTeX. These classes are never instantiated anywhere in the codebase
- [low/dead_code/risk:low/conf:medium] **field_theory.py:190-199** — _poly_taylor() is only called from the 'taylor_coeffs' legacy path (lines 1346-1361), which itself is dead code. The comment explicitly labels this as 'legacy path'. No active models in the repo use '
- [low/dead_code/risk:none/conf:high] **propagator.py:1-9** — propagator.py is a stub file containing only a docstring and no implementation. The docstring admits 'Status: Not yet extracted — logic currently lives in the demo notebook.' Actual propagator code re
- [low/simplification/risk:none/conf:high] **serialize.py:28-42** — _strip_callables() uses a verbose nested-loop pattern when a dict comprehension would be clearer. Current code manually filters by type in an if-condition, resulting in redundant iteration.
- [low/complexity/risk:low/conf:medium] **vertices.py:327-345** — _parse_field_name() manually scans a string backwards to split base name from trailing digits. The implementation is readable but verbose and silently accepts malformed input (e.g., 'abc12def' returns
- [low/complexity/risk:low/conf:medium] **causality.py:57-77** — check_pole_structure has three separate try-except-pass blocks (lines 58-77) that test im_simplified against three conditions (> 0, <= 0, == 0) in sequence, each with identical exception handling. The
- [low/inconsistency/risk:none/conf:high] **causality.py:19-28** — The `omega` parameter to check_pole_structure is documented as 'The frequency variable (unused, kept for interface consistency)' but the parameter is never read and does not appear in the function bod
- [low/duplication/risk:none/conf:high] **filter.py:12-40** — classify_prediagram_vertices logic is duplicated inline in enumerate_typed_diagrams (type_assignment.py, lines 167-177). Both iterate D.vertices(), build leaf_set, and classify into source_verts and i
- [low/simplification/risk:low/conf:medium] **symmetry.py:527-550** — deduplicate_typed_diagrams is a thin wrapper around deduplicate_with_multiplicities that discards the multiplicities return value (line 549: `unique, _mult = ...`). The function exists solely to provi
- [low/simplification/risk:none/conf:high] **full_integrator.py:325-333** — Lines 328-332 compute the double factorial (k-1)!! via a while loop initializing df=1.0 and decrementing kk by 2. This is a manual implementation of a standard mathematical operation. The loop is clea
- [low/docs/risk:none/conf:medium] **generic_evaluator.py:1-33** — The module header states '⚠ ORACLE-ONLY — not on the production path. Superseded by full_integrator.py ... reached only by its own test(s).' However, the module contains a full suite of public functio
- [low/dead_code/risk:none/conf:high] **heat_kernel.py:44** — Imports Callable and Optional from typing but never uses them anywhere in the file. The codebase uses modern Python annotations with | syntax (e.g., 'float | None') and does not apply Callable/Optiona
- [low/dead_code/risk:none/conf:high] **pipeline_bridge.py:782** — The backward-compatibility alias `bubble_loop_form_factor = diagram_form_factor` (line 782) is defined but is NEVER imported or called anywhere in the repository. It exists only as a deprecation shim 
- [low/simplification/risk:none/conf:high] **pipeline_bridge.py:1783-1785** — In `compute_spatial_correlator_generic`, the adaptive grid selection logic appears twice with identical hardcoded quadrature parameters: lines 1368-1372 in `compute_coupled_loop_correlator` and lines 
- [low/simplification/risk:low/conf:medium] **pipeline_bridge.py:976-1030** — The `_formfactor_callable` function (lines 958-1055) has substantial duplication in the d=1 and d≥2 branches. Both branches: (1) call `diagram_form_factor` to get the symbolic form, (2) lambdify it fo
- [low/dead_code/risk:none/conf:high] **spatial_correlator.py:34** — Imports cmath at the module level but never uses it anywhere in the file. Module uses only the math library for arithmetic operations (sqrt, pi, exp, etc.) and scipy.special for specialized functions 
- [low/simplification/risk:none/conf:high] **spatial_correlator.py:37, 190, 314, 356** — numpy is imported at the module level (line 37) as 'np', but then re-imported locally inside three functions: compute_spatial_correlator_tree (line 190), radial_inverse_ft (line 314), and periodic_inv
- [low/duplication/risk:low/conf:high] **final_integral.py:4186-4288 and 4291-4365** — The two fast-subset evaluators `_build_fast_subset_evaluator` and `_build_fast_subset_evaluator_from_modes` are nearly identical: both build edge-data tuples from per-edge (poles, residues, c0, int_pa
- [low/duplication/risk:none/conf:high] **final_integral.py:3944-3967 and 3970-4000** — In `eval_delta_contributions_on_tau_grid`, the inner loop that evaluates `coeff_fc` and checks retardation constraints appears in TWO places: once inside the 'degenerate' case (lines 3944–3966) and on
- [low/duplication/risk:none/conf:high] **final_integral.py:4078–4104 and 4114–4177** — In `eval_delta_contributions_on_2d_grid`, the handling of the special case where the delta equality has zero dependence on one or both grid axes (lines 4074–4104) duplicates logic that is structurally
- [low/simplification/risk:none/conf:high] **final_integral.py:4769-4796** — The conditional on line 4773 `needs_filter = (not pure_s1_found) or DEBUG_HEAVISIDE_GUARD` controls whether to wrap the integrand in Heaviside filtering. Then, two nearly identical function definition
- [low/duplication/risk:low/conf:medium] **grouped_integral.py:531-548** — The vertex-time lookup pattern repeats for u and v: `if u in vertex_leg_time: { look up edge_pop_idx, call .get(..., vertex_time[u]) } else: t_u = vertex_time[u]`. Lines 533-540 do this for u; lines 5
- [low/duplication/risk:none/conf:high] **pipeline.py:445-447, 543-545** — The fork-in-notebook guard pattern `if parallel and _fork_unsafe_in_notebook(start_method): _warn_fork_guard_once(); parallel = False` appears at lines 445-447 in `total_C_batch` and lines 543-545 in 
- [low/simplification/risk:none/conf:high] **propagator_td.py:225-233** — The _to_sr_ab helper has a three-way branch: complex, ComplexDoubleElement, or other. The 'complex' case (lines 227-228) converts to float then SR, and the ComplexDoubleElement case (lines 229-231) do
- [low/dead_code/risk:none/conf:high] **subgraph.py:66-119** — The function `identify_loop_subgraphs` always returns `[]` for tree-level diagrams (line 111, the MVP case) and raises `NotImplementedError` for loop diagrams (lines 113-118). The entire function body
- [low/inconsistency/risk:none/conf:medium] **loop_dyson.py:64,74,102,105,128** — Multiple hardcoded scipy.integrate limits: limit=120 (lines 64, 74) for pole-free Gaussian integrals in sigma_R_time/sigma_K_time; limit=200 (lines 102, 105, 128) for higher-dimensional/more complex i
- [low/inconsistency/risk:low/conf:medium] **loop_parametric.py:105,118** — Hardcoded limit=200 in sigma_R_kernel and implicit default limits in sigma_K_kernel (dblquad). Compare with loop_dyson where sigma_R_time/sigma_K_time use limit=120 for 1-D integrals. The choice of 20
- [low/docs/risk:none/conf:low] **spatial_reduce.py:1-40** — Docstring notation inconsistency: C0/C1 are used as section labels in the docstring (lines 4-5) but internal comments use different notation. Also, the Symanzik notation section (lines 47-54) uses bot
- [low/simplification/risk:none/conf:high] **temporal_integrate.py:191** — Redundant import of math module inside bubble_delta_phi2_via function. The math module is already imported at module level (line 45), so the late import at line 191 is unnecessary.
- [low/simplification/risk:none/conf:high] **temporal_integrate.py:107** — Late import of itertools inside sigma_parametric() at the nC >= 2 branch. Unlike temporal_integrate (which does late-import), all other spatial modules import itertools at module level. This creates i
- [low/complexity/risk:low/conf:medium] **temporal_integrate.py:56-123** — The quad_opts parameter (line 56) is documented and passed to the function, but is only used when nC == 1 (line 98, passed to integrate.quad). When nC == 0, _integrand([]) is called directly with no i
- [low/dead_code/risk:none/conf:high] **_expand_cache.py:343** — The 'cache_version' key is written into the saved bundle in save_expand() (line 343), but it is never read or checked anywhere in load_expand() or any other function in this module. A grep search conf
- [low/simplification/risk:none/conf:high] **_mean_field.py:193, 211** — Inconsistent math.isfinite() checks: lines 193 and 211 use __import__('math').isfinite() inline, while line 505 in _solve_mean_field_hetero imports math as a module and uses math.isfinite() directly. 
- [low/simplification/risk:none/conf:medium] **_mean_field.py:165-170** — Initial guess list construction is verbose and the ordering of [0.1, 0.5, 1.0, 0.01] could be clearer. The sequence is chosen heuristically but the rationale isn't documented inline.
- [low/simplification/risk:low/conf:high] **_mean_field_dae.py:203-216** — _numpy_params() function converts fundamental parameters to numpy arrays. The logic (lines 209-215) checks indexed_by and isinstance checks, but the decision tree is redundant: if indexed_by exists, c
- [low/dead_code/risk:none/conf:high] **_propagator.py:1068-1070** — In the heterogeneous/rich path, _G_at() (lines 1066-1070) is defined and wraps _K_at() inversion to return G = K^{-1}. However, a grep of the entire compute_poles_and_residues function shows this func
- [low/simplification/risk:low/conf:medium] **colored_to_markovian.py:338-346** — In markovianize_spec(), the response_to_natural dict is built via a nested loop and conditional logic (lines 304-317), then aux_natural_by_source is populated by iterating source_field_names and looki
- [low/duplication/risk:none/conf:medium] **compute.py:45-47** — The _trunc() helper in compute.py (lines 45-47) truncates strings to a max length with '...' suffix. A nearly identical function _trunc_str() exists in _propagator.py (lines 316-318) with the same pur
- [low/simplification/risk:none/conf:high] **compute.py:878** — Line 878 checks `if not all(v != v for v in vals)` to detect whether all elements are NaN. This is a cryptic double-negative that exploits NaN's self-inequality (NaN != NaN is True). The idiom works b
- [low/dead_code/risk:none/conf:high] **theory.py:1231-1233** — Three blank lines between section comments (lines 1231-1233). While not technically dead code, this is excessive whitespace that may indicate incomplete refactoring or sections that were intended to h
- [low/complexity/risk:low/conf:medium] **theory.py:673** — In `_TemporalMethods.add_gtas_noise()` at line 673, the logic stores the template in `self._pending_gtas`, then checks for it later in the **second** `use_action_template` definition (line 686) with `
- [low/simplification/risk:none/conf:medium] **theory_compiler.py:1320-1322** — In `make_mf_bg_conditions_lambda()` at lines 1320-1322, the code iterates `for i in range(len(saddle_array))` but adds a comment explaining why (to handle per-saddle size vs n_pop). This is correct, b
- [medium/docs/risk:none/conf:high] **field_theory.py:1010-1013** — free_action() docstring states 'Return the (1,1) sector polynomial' but the code returns self._R.zero() if the sector doesn't exist. The docstring omits the fallback behavior, causing potential confus
- [medium/simplification/risk:low/conf:high] **type_assignment.py:22-24** — _distinct_permutations wrapper is misleading. It returns `set(permutations(seq))`, which does NOT produce genuinely distinct permutations of a multiset (e.g., seq=[1,1,2] yields 3 results, not 2). The
- [medium/duplication/risk:low/conf:high] **full_integrator.py:517-536, 918-934** — The Schwinger parameter grid construction (σ-grid) is duplicated almost verbatim in both diagram_kinematic (lines 517-536, with e^{-μσ} folded into s_w) and diagram_kinematic_spectral (lines 918-934, 
- [medium/duplication/risk:low/conf:high] **full_integrator.py:580-603, 941-963** — The deterministic causal-chamber product grid construction is nearly identical in both diagram_kinematic (lines 580-603) and diagram_kinematic_spectral (lines 941-963). Both loop over reversed(order),
- [medium/dead_code/risk:none/conf:high] **heat_kernel.py:287-306** — Function _make_numeric has a formal parameter 'free_symbol_names' that is documented in the docstring but completely unused in the function body. The function instead extracts symbols directly via exp
- [medium/dead_code/risk:none/conf:high] **pipeline_bridge.py:1074-1078** — The function `_live_bubbles` is defined but never referenced anywhere in the codebase (including all test files, scratch files, and archive). It performs filtering of bubble diagrams by checking if th
- [medium/duplication/risk:low/conf:high] **final_integral.py:2047-2105 (and similar at 2120-2172)** — The `_integrate_1d_polytope_modesum` function duplicates nearly identical 60-line code blocks: one for the plan-cache fast path (if plan is not None) and another for the legacy path (no plan). Both bl
- [medium/duplication/risk:low/conf:high] **final_integral.py:2238-2273 (and similar at 2275-2330)** — The `_integrate_2d_polygon_modesum` function similarly duplicates ~95 lines of pole-tuple processing between the plan-cache path and legacy path. Both paths build the polygon once, then iterate over p
- [medium/duplication/risk:low/conf:high] **final_integral.py:1865-1911 (and similar at 1913-1973)** — The `_integrate_nd_polytope_poset_modesum` function has a ~50-line plan-cache path (lines 1865–1911) and a ~60-line legacy path (lines 1913–1973). Both enumerate linear extensions and loop over pole t
- [medium/duplication/risk:none/conf:high] **grouped_integral.py:150-164** — The pattern `float(c0) + sum(float(a_ext[i]) * float(free_ext_vals[i]) for i in range(len(a_ext)))` appears identically at lines 151-153 (in a loop checking c_eff) and 159-161 (in list comprehension c
- [medium/duplication/risk:low/conf:high] **grouped_integral.py:150-176, 207-228** — Functions `_evaluate_grouped_m0_modesum` (lines 138-176) and `_integrate_grouped_m1_modesum` (lines 179-280) both compute constraint-based time differences from the same `subset_constraint_data` forma
- [medium/duplication/risk:medium/conf:medium] **pipeline.py:441-510, 531-573** — `total_C_batch` (lines 384-510) and `eval_per_diagram_batch` (lines 512-573) share almost identical structure: (1) convert tau_points to tau_list, (2) check fork-in-notebook guard, (3) compute task co
- [medium/dead_code/risk:none/conf:high] **loop_dyson.py:46** — The helper function _mk(k, mu, D) is defined at line 46 but is ONLY referenced within this oracle-only module (used in sigma_R_time, sigma_K_time, _dyson_terms, bubble_delta_C_q_tau). Since loop_dyson
- [medium/duplication/risk:low/conf:high] **_mean_field.py:54-87** — Parameter substitution logic duplicated in _solve_mean_field (legacy single-pop) and _solve_mean_field_hetero. Both implement the same indexed_by → SR.var(f'{pname}{i+1}{j+1}') pattern for 2D, 1D, and
- [medium/duplication/risk:none/conf:high] **_propagator.py:1048-1056, 1251-1258, 1546-1551** — The _horner() polynomial-evaluation helper is defined identically (with minor formatting differences) in three separate scopes within compute_poles_and_residues(). The first two instances (lines 1048,
- [medium/duplication/risk:none/conf:high] **_propagator.py:1477-1487, 1564-1572** — The _cofactor_adj() helper is defined identically in two adjacent scopes: once in the heterogeneous/rich path (line 1477, named _cofactor_adj) and again in the lean symbolic path (line 1564, named _co
- [medium/complexity/risk:none/conf:medium] **spatial_operator_ir.py:275-342** — The `to_derived_generators()` function performs a multi-pass lowering (find innermost node → record chain → substitute) using a `while True` loop and global counters. The logic correctly handles compo
- [medium/complexity/risk:none/conf:high] **theory.py:1315-1377** — The `_auto_populate_mf_eqs_from_equations()` method contains a complex state-machine of regex substitutions to derive legacy MF equations from the new DAE `.equation()` API. The logic at lines 1334-13
- [medium/duplication/risk:low/conf:high] **theory.py:1640-1658** — In the `_compile_text_declarations()` method, the matrix-parameter substitution lambda (lines 1640-1658) duplicates the same indexing-resolution logic that appears elsewhere in the file (e.g., in `mak
